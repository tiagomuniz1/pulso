DB_HOST=localhost
DB_PORT=5499
DB_USER=postgres
DB_PASS=postgres
DB_NAME=app
DB_SCHEMA=dev
REDIS_HOST=localhost
REDIS_PORT=6399
JWT_SECRET=local-dev-jwt-secret-change-in-production
JWT_EXPIRATION=900s
JWT_REFRESH_EXPIRATION=7d
FRONTEND_URL=http://localhost:3000
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_FROM=noreply@pulso.center

# Bucket de staging (infra/terraform/environments/staging), reaproveitado localmente.
# Precisa de credenciais AWS válidas do profile umi-homologation (conta 796669927752) —
# rode: aws sso login --profile umi-homologation (ou aws configure --profile umi-homologation)
# e depois: export AWS_PROFILE=umi-homologation antes de subir o backend.
AWS_S3_BUCKET=clinic-assets-staging
AWS_REGION=us-east-1
